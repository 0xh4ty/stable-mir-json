[alias]
# Build the mir-explorer WASM crate
wasm-dev = "build -p mir-explorer --target wasm32-unknown-unknown"
wasm-release = "build -p mir-explorer --target wasm32-unknown-unknown --release"
wasm-check = "check -p mir-explorer --target wasm32-unknown-unknown"
